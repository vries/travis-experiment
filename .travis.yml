os:
- linux

language: python
python:
  - "2.7"
  - "3.6"

install:
  - pip install six pygments graphviz lxml

script: python --version; python -c "import weakref"; python -c "import pathlib, weakref; print(pathlib.Path(weakref.__file__).resolve().parent)"

