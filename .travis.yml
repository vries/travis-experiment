os:
- linux

language: python
python:
  - "2.7"
  - "3.6"

install:
  - pip install six pygments graphviz lxml

script: virtualenv --version; python -v -c "import weakref; print(weakref.__file__); find ~/virtualenv/python2.7/"
